DATABASE_URL="postgresql://deepwiki:deepwiki@localhost:5433/deepwiki?schema=public"
PORT=4000
WEB_ORIGIN=http://localhost:5173
# Max parallel page-generation agent runs per repo index
INDEX_CONCURRENCY=4
# Hard timeout for a single agent invocation (ms)
AGENT_TIMEOUT_MS=900000
